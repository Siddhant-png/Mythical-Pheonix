import React, { useEffect, useState } from 'react';
import { ArrowLeft, ArrowRight, Check, Eye, EyeOff, KeyRound, LockKeyhole, Mail, ShieldCheck, Sparkles, User, X } from 'lucide-react';
import { AuthUser, UserRole } from '../types';
import { loginWithPassword, registerWithPassword, requestPasswordReset, resetPasswordWithCode } from '../services/api';

interface AuthModalProps { isOpen: boolean; onClose: () => void; onLoginSuccess: (user: AuthUser) => void; initialRole?: UserRole; }
type Screen = 'login' | 'register' | 'forgot' | 'reset' | 'success';
const roleLabels: Record<UserRole, string> = { dept: 'Government', startup: 'Startup', citizen: 'Citizen', manufacturer: 'Manufacturer' };

export const AuthModal: React.FC<AuthModalProps> = ({ isOpen, onClose, onLoginSuccess, initialRole = 'citizen' }) => {
  const [screen, setScreen] = useState<Screen>('login'); const [role, setRole] = useState<UserRole>(initialRole);
  const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [code, setCode] = useState('');
  const [showPassword, setShowPassword] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState('');
  useEffect(() => { if (isOpen) { setScreen('login'); setError(''); setPassword(''); setCode(''); } }, [isOpen]);
  if (!isOpen) return null;
  const changeScreen = (next: Screen) => { setError(''); setScreen(next); };
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(''); setLoading(true); try {
    if (screen === 'login') { const user = await loginWithPassword(email, password); onLoginSuccess(user); changeScreen('success'); setTimeout(onClose, 900); }
    else if (screen === 'register') { const user = await registerWithPassword(name, email, password, role); onLoginSuccess(user); changeScreen('success'); setTimeout(onClose, 900); }
    else if (screen === 'forgot') { await requestPasswordReset(email); changeScreen('reset'); }
    else if (screen === 'reset') { const user = await resetPasswordWithCode(email, code, password); onLoginSuccess(user); changeScreen('success'); setTimeout(onClose, 900); }
  } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to continue.'); } finally { setLoading(false); } };
  const reset = screen === 'reset'; const title = screen === 'login' ? 'Welcome back' : screen === 'register' ? 'Create your account' : screen === 'forgot' ? 'Reset your password' : screen === 'reset' ? 'Choose a new password' : 'You’re all set';
  const subtitle = screen === 'login' ? 'Sign in to your Converge workspace.' : screen === 'register' ? 'Join Maharashtra’s innovation network.' : screen === 'forgot' ? 'We’ll help you safely get back in.' : screen === 'reset' ? 'Enter the code and choose something new.' : 'Your secure session is ready.';
  return <div className="fixed inset-0 z-50 flex items-center justify-center overflow-y-auto bg-slate-950/70 p-4 backdrop-blur-lg animate-in fade-in duration-300">
    <div className="relative w-full max-w-5xl overflow-hidden rounded-[2rem] border border-white/20 bg-white shadow-2xl animate-in zoom-in-95 slide-in-from-bottom-4 duration-500">
      <button onClick={onClose} className="absolute right-4 top-4 z-10 rounded-full bg-white/10 p-2 text-white transition hover:bg-white/20"><X className="h-5 w-5" /></button>
      <div className="grid min-h-[590px] md:grid-cols-[1.05fr_1fr]"><aside className="relative hidden overflow-hidden bg-gradient-to-br from-[#071b3d] via-[#0f4c81] to-[#0f766e] p-10 text-white md:block"><div className="absolute -right-24 -top-24 h-64 w-64 rounded-full bg-amber-400/25 blur-3xl animate-pulse" /><div className="absolute -bottom-24 -left-16 h-64 w-64 rounded-full bg-fuchsia-500/20 blur-3xl" /><div className="relative flex h-full flex-col justify-between"><div><div className="mb-10 inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-3 py-2 text-xs font-bold"><ShieldCheck className="h-4 w-4 text-amber-300" /> CONVERGE ACCESS</div><h2 className="max-w-sm text-4xl font-black leading-tight">Where public innovation finds momentum.</h2><p className="mt-5 max-w-sm text-sm leading-6 text-sky-100">A trusted gateway for builders, partners, departments and citizens shaping Maharashtra’s next chapter.</p></div><div className="space-y-3 text-sm"><div className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-full bg-amber-400 text-slate-950"><Check className="h-4 w-4" /></span> One secure identity</div><div className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-full bg-emerald-400 text-slate-950"><Sparkles className="h-4 w-4" /></span> Built for meaningful work</div></div></div></aside>
        <main className="flex items-center p-7 sm:p-10"><div className="mx-auto w-full max-w-sm"><div className="mb-7 md:hidden"><div className="inline-flex items-center gap-2 rounded-full bg-slate-900 px-3 py-2 text-xs font-bold text-white"><ShieldCheck className="h-4 w-4 text-amber-300" /> CONVERGE ACCESS</div></div>{screen === 'success' ? <div className="py-16 text-center animate-in zoom-in duration-500"><div className="mx-auto mb-5 grid h-16 w-16 place-items-center rounded-full bg-emerald-500 text-white shadow-lg shadow-emerald-200"><Check className="h-9 w-9" /></div><h1 className="text-2xl font-black text-slate-900">Welcome to Converge</h1><p className="mt-2 text-sm text-slate-500">Your account is ready to go.</p></div> : <><div className="mb-7"><h1 className="text-3xl font-black tracking-tight text-slate-900">{title}</h1><p className="mt-2 text-sm text-slate-500">{subtitle}</p></div><form onSubmit={submit} className="space-y-4"><div className="animate-in fade-in slide-in-from-right-3 duration-300" key={screen}>{screen === 'register' && <><label className="mb-1.5 block text-xs font-bold text-slate-700">How should we call you?</label><div className="relative mb-4"><User className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><input value={name} onChange={e => setName(e.target.value)} required placeholder="Your full name" className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-3 text-sm outline-none transition focus:border-sky-500 focus:ring-4 focus:ring-sky-100" /></div><label className="mb-1.5 block text-xs font-bold text-slate-700">I’m joining as</label><div className="mb-4 grid grid-cols-2 gap-2">{(Object.keys(roleLabels) as UserRole[]).map(item => <button type="button" key={item} onClick={() => setRole(item)} className={`rounded-xl border px-2 py-2 text-xs font-bold transition ${role === item ? 'border-sky-500 bg-sky-50 text-sky-800 ring-2 ring-sky-100' : 'border-slate-200 text-slate-500 hover:border-slate-300'}`}>{roleLabels[item]}</button>)}</div></>}<label className="mb-1.5 block text-xs font-bold text-slate-700">Email address</label><div className="relative"><Mail className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><input type="email" value={email} onChange={e => setEmail(e.target.value)} required placeholder="you@example.com" className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-3 text-sm outline-none transition focus:border-sky-500 focus:ring-4 focus:ring-sky-100" /></div>{reset && <><label className="mb-1.5 mt-4 block text-xs font-bold text-slate-700">6-digit reset code</label><div className="relative"><KeyRound className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><input value={code} onChange={e => setCode(e.target.value)} maxLength={6} required placeholder="123456" className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-3 text-sm tracking-[0.3em] outline-none transition focus:border-sky-500 focus:ring-4 focus:ring-sky-100" /></div><p className="mt-2 text-xs text-amber-700">Demo delivery code: 123456</p></>}{screen !== 'forgot' && <><label className="mb-1.5 mt-4 block text-xs font-bold text-slate-700">Password</label><div className="relative"><LockKeyhole className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><input type={showPassword ? 'text' : 'password'} value={password} onChange={e => setPassword(e.target.value)} minLength={8} required placeholder="At least 8 characters" className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-10 text-sm outline-none transition focus:border-sky-500 focus:ring-4 focus:ring-sky-100" /><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-3 text-slate-400">{showPassword ? <EyeOff className="h-4 w-4" /> : <Eye className="h-4 w-4" />}</button></div></>}</div>{error && <p className="rounded-xl bg-rose-50 px-3 py-2 text-xs font-medium text-rose-700">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-[#0b3470] via-[#1268a8] to-[#0f8b7e] py-3 text-sm font-bold text-white shadow-lg shadow-sky-200 transition hover:brightness-110 disabled:opacity-60">{loading ? 'Please wait…' : screen === 'login' ? 'Sign in' : screen === 'register' ? 'Create account' : screen === 'forgot' ? 'Send reset code' : 'Reset password'}<ArrowRight className="h-4 w-4" /></button></form><div className="mt-5 flex items-center justify-between text-xs font-semibold"><button onClick={() => changeScreen(screen === 'login' ? 'register' : 'login')} className="text-sky-700 hover:text-sky-900">{screen === 'login' ? 'Create an account' : 'Already have an account?'}</button>{screen === 'login' && <button onClick={() => changeScreen('forgot')} className="text-slate-500 hover:text-slate-900">Forgot password?</button>}{(screen === 'forgot' || screen === 'reset') && <button onClick={() => changeScreen('login')} className="inline-flex items-center gap-1 text-slate-500 hover:text-slate-900"><ArrowLeft className="h-3 w-3" /> Back to sign in</button>}</div></>}</div></main></div>
    </div>
  </div>;
};
